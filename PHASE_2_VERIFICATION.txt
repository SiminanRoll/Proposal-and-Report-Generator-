Proposal & Report Generator — Phase 2 Static Verification

Scope
- Next.js + TypeScript static web source
- Three project intake paths preserved
- RFT, PDF, DOCX, TXT, and image handling in the browser
- Structured facts, evidence, findings, confidence, and focused exceptions
- Browser-local structured persistence plus IndexedDB source-file caching
- Explicit JSON backup and restore for structured projects
- No API route, Dockerfile, Python runtime, launcher, database, or hosted source storage

static hosting
- Resource type: Static Site
- Build command: npm run build
- Output directory: out
- Route: /
- No run command, port, health check, database, or object storage

Validation completed in the build environment
- 18 automated contract and privacy tests passed
- 21 TypeScript/TSX source files passed syntax validation
- Internal TypeScript relationships passed strict validation with external dependency stubs
- All local @/ imports resolved
- Project JSON schema parsed successfully
- No prohibited launcher, Python, Docker, or API-route files found

Full dependency build
The restricted build environment cannot download the npm dependency set. After uploading, run:
  npm install
  npm run check

A successful build creates the deployable static site in:
  out/
