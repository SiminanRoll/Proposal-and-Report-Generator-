# Header logo update

- Keeps the standalone blue Advantage mark once.
- Uses the supplied Advantage Technologies wordmark without the duplicate mark.
- Removes the stray blue edge fragment from the supplied asset.
- Preserves the simplified static header and local-only processing architecture.
