Client Compass v1.1.0 — manual commit hotfix bundle

Base:
  main @ 8aaa4733aea94cf279a0565d3556af4d2aa2b7fd

Purpose:
  Replaces the 10 remaining stale regression test files reported by the failed Quality run after the 1.1.0 release alignment.

Use:
  Extract this ZIP at the repository root and allow the included tests/ files to overwrite the matching files.
  Then commit/push normally.

This bundle does not change application/runtime code or package-lock.json.
