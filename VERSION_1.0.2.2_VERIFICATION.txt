Version 1.0.2.2 verification targets

1. A workstation stored with age "2.2 years" and an old Replace Now status is normalized to Healthy Now.
2. A row stored as a workstation but modeled as a Virtual Machine is excluded from workstation/server totals.
3. Duplicate inventory rows do not inflate lifecycle totals.
4. Healthy Now + Plan Soon + Replace Now equals Total Assets whenever named physical inventory is available.
5. Legacy warranty/OS summary panels remain hidden.
6. Live, HTML, and PDF priority cards use dark backgrounds, including server-first cards.

Completed verification
- 83 automated tests passed; 0 failed.
- 37 TypeScript/TSX source files transpiled with 0 syntax diagnostics.
- Targeted regression confirms 13 imported rows containing 2 VMs produce 11 physical assets.
- Targeted regression confirms a stored 2.2-year workstation is Healthy Now and absent from replacement lists.
