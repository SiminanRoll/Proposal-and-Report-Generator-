Proposal and Report Generator v1.0.1.6

Verification targets:
- Finished Package and live Presentation both expose Download PDF.
- Download PDF opens a local print-ready document and automatically launches window.print().
- Print CSS requests a landscape page and starts each major report section on a new page.
- Presentation mode sets a client-facing document title and restores the previous title on close.
- Downloaded interactive HTML also uses the client-facing document title.
- PDF generation adds no server route, upload, or hosted client-data dependency.

Validation completed:
- 69 automated tests passed.
- 37 TypeScript/TSX/config files passed syntax transpilation.
- ZIP integrity verified.
- Full dependency-backed Next.js typecheck/build was not available in this environment because package dependencies are not installed.
